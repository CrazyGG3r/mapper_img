{
  "_comment": "Machine-readable mirror of docs/architecture.md's binding dependency table. Each key is an @topview/* package name; its value is the exhaustive list of @topview/* packages it may declare as a runtime dependency. Checked by scripts/verify-dependency-graph.mjs (npm run verify-deps).",
  "@topview/schema": [],
  "@topview/plugin-sdk": ["@topview/schema"],
  "@topview/geometry-wasm": ["@topview/schema"],
  "@topview/svg-engine": ["@topview/schema", "@topview/geometry-wasm"],
  "@topview/pipeline-core": ["@topview/schema", "@topview/plugin-sdk", "@topview/svg-engine"],
  "@topview/plugin-example-detector": ["@topview/schema", "@topview/plugin-sdk"],
  "@topview/web": [
    "@topview/schema",
    "@topview/plugin-sdk",
    "@topview/pipeline-core",
    "@topview/svg-engine",
    "@topview/geometry-wasm",
    "@topview/plugin-example-detector"
  ]
}
