{
  "name": "@topview/pipeline-core",
  "private": true,
  "version": "0.1.0",
  "description": "Orchestrates the 11-stage TopView reconstruction pipeline: invokes registered plugins per stage, bridges plugin-sdk's provisional DetectedEntity shape into schema's authoritative AnyEntity project model, and emits the PipelineEvent progress protocol.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@topview/schema": "*",
    "@topview/plugin-sdk": "*",
    "@topview/svg-engine": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "rimraf": "^6.0.0"
  }
}
