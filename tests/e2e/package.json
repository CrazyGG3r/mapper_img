{
  "name": "topview-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Black-box Playwright e2e suite driving a built/running apps/web over HTTP. Not an npm workspace member (excluded from the root [\"apps/*\",\"packages/*\",\"plugins/*\"] globs) and imports nothing from apps/web/src.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
