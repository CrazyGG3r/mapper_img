# services/reconstruction-api/requirements.txt
#
# Core web service. Kept deliberately light — this is a stub/scaffold service
# (see README.md). Heavy geometry/vision libraries that real stage handlers
# will eventually need are listed further down, commented out, so a future
# implementer opts into them deliberately rather than paying multi-hundred-MB
# install costs (and, for pycolmap/open3d, native-toolchain / GPU headaches)
# just to boot the API stub.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
numpy>=2.0
opencv-python>=4.10
python-multipart>=0.0.9

# --- Optional / heavy — uncomment only when implementing the real stage ---
# handlers behind app/routers/sfm.py, dense_reconstruction.py, and
# ai_inference.py. These are NOT required to boot the API stub, run the
# health check, or exercise the 501 stub responses.
#
# pycolmap>=0.6            # structure-from-motion / camera pose estimation
#                           # (native extension; wheels are large and
#                           # platform-specific — see docs/installation.md)
# open3d>=0.18              # dense point-cloud / mesh processing, MVS helpers
#                           # (large wheel, pulls in its own vendored OpenGL /
#                           # Qt bits; not needed for the stub endpoints)
# torch>=2.4                # only if/when ai_inference.py loads a real model
#                           # (CPU or CUDA build — pick per deployment target)
# onnxruntime>=1.19         # lighter-weight alternative to torch for serving
#                           # exported inference models, if that path is chosen
