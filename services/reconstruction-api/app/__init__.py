"""reconstruction_api — TopView's heavy-compute backend service (stub).

Importable as the `app` package (see Dockerfile / uvicorn entrypoint
`app.main:app`). See README.md in this directory for scope, status, and the
explicit "unverified in this environment" caveat.
"""

__version__ = "0.1.0"
